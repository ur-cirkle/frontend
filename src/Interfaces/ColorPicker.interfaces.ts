export interface colorsState {
  hex: {
    hexCode: string;
    opacity: number;
  };
  rgba: {
    red: number;
    green: number;
    blue: number;
    alpha: number;
  };
  hsl: {
    hue: number;
    saturation: number;
    lightness: number;
  };
}
